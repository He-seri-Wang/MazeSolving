# Project

This project contains Arduino code and native unit tests.

Running native tests
--------------------

Use the included script to compile and run native tests (does not require PlatformIO native board):

```bash
./run_tests.sh
```
